<tr>
  <td><%= @projects.name %></td>
  <td><%=link_to 'Show', @project, user_project_path(@user, @project)  %></td>
  <td><%= link_to 'Edit', edit_user_project_path(project) %></td>
  <td><%= link_to 'Destroy',project, method: :delete, data: {confirm: 'Are you sure?'} %></td>
</tr>